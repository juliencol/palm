<% @islands.each do |island| %>
          <div>
            <h2><%= island.name %></h2>
            <h3> <%= island.location%></h3>
            <h3> <%= island.description %></h3>
          </div>
<% end %>
